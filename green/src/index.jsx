import React from 'react';
import ReactDOM from 'react-dom';
import DynamicListManager from './App';

ReactDOM.render(
  <React.StrictMode>
    <DynamicListManager />
  </React.StrictMode>,
  document.getElementById('root')
);